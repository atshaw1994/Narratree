<%= render partial: "shared/section_container", locals: {
    header: content_tag(:h2, "Community Guidelines"),
    body: content_tag(:div, style: "display: flex; flex-direction: column; align-items: center; width: 100%;") do
        content_tag(:div, style: "max-width: 700px; width: 100%; text-align: left;") do
            safe_join([
                content_tag(:h4, "Welcome to Narratree!"),
                content_tag(:p, "To ensure a positive experience for all members, please adhere to the following guidelines:"),
                content_tag(:ul, style: "list-style-type: disc; padding-left: 20px;") do
                    safe_join([
                        content_tag(:li, "Be respectful and considerate to all members."),
                        content_tag(:li, "No hate speech, harassment, or discrimination."),
                        content_tag(:li, "Do not post illegal, explicit, or violent content."),
                        content_tag(:li, "Respect privacy — do not share personal information without consent."),
                        content_tag(:li, "Report any violations to the administrators."),
                        content_tag(:li, "Follow all applicable laws and platform rules."),
                        content_tag(:li) do
                            safe_join([
                                "Absolutely NO political content or discussions.",
                                content_tag(:ul) do
                                    safe_join([
                                        content_tag(:li) do
                                            safe_join([
                                                content_tag(:p, "Though I (the owner) support political discourse, it must be kept off of this platform.")
                                            ])
                                        end
                                    ])
                                end
                            ])
                        end
                    ])
                end,
                content_tag(:p, "<strong>Violations may result in content removal or account suspension.</strong>".html_safe),
                content_tag(:p, "<strong>Thank you for helping us build a safe and positive community!</strong>".html_safe)
            ])
        end
    end
} %>
